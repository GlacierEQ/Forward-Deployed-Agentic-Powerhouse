# Forward-Deployed-Agentic-Powerhouse

```text
DISCOVER → FRAME → BUILD → INTEGRATE → EVALUATE → PROVE → DEPLOY
```

**Identity:** Forward Deployed Agentic AI  
**Version:** 0.6.1 · **main** · **9-range CI**  
**Law:** Full power + dual-plane honesty · `approval_packet_only`

---

## One command

```bash
pip install -e ".[dev]"
python -m fde_powerhouse maximize
pytest --cov=fde_powerhouse --cov-fail-under=70
```

---

## 9+ CI closeout

| Item | Status |
|------|--------|
| Pinned mega-skills validate | `configs/pins.yaml` + CI job `mega-skills-pinned` |
| Coverage gate | `--cov-fail-under=70` on 3.11–3.13 |
| Scheduled edge artifacts | Weekly + `workflow_dispatch`, 90-day retention |

See [docs/CI_AND_PINS.md](docs/CI_AND_PINS.md).

Private mega-skills checkout: set secret **`ESTATE_CHECKOUT_TOKEN`**.

---

## Surface

| Command | Power |
|---------|-------|
| `maximize` | Full one-shot |
| `invoke` | Live mega-skills (validate-only default) |
| `genius --synthesize` | Live Genius forge |
| `edge-probe --persist` | Probe + history store |
| `scan --target` | Upgrade inventory |

```bash
export FDE_PATH_MEGA_SKILLS=/path/to/mega-skills
export FDE_PATH_GENIUS_MASTERY=/path/to/Genius-Mastery
python -m fde_powerhouse maximize
```

---

## Estate

709 atomic · 33 compound · 29 mega · 10 pipelines · Genius v1.1.0 · 7-gate · 40+ edge · 9 modes

---

*GlacierEQ · Forward Deployed Agentic AI · 9-range*
