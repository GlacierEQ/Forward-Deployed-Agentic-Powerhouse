# Forward-Deployed-Agentic-Powerhouse

```text
DISCOVER → FRAME → BUILD → INTEGRATE → EVALUATE → PROVE → DEPLOY
```

**Identity:** Forward Deployed Agentic AI  
**Version:** 0.6.0 · **MAXIMIZE** · **main**  
**Law:** Full power + dual-plane honesty · `approval_packet_only`

---

## One command

```bash
pip install -e ".[dev]"
python -m fde_powerhouse maximize
pytest -q
```

---

## Full surface

| Command | Power |
|---------|-------|
| **`maximize`** | Doctor → proof → showcase → ground_up → upgrade → scan → leverage → invoke → genius → edge store |
| `genius --synthesize` | Live Genius forge when `FDE_PATH_GENIUS_MASTERY` set |
| `invoke` | Live mega-skills validate / execute |
| `edge-probe --persist` | Probe + historical receipt store |
| `scan --target PATH` | Upgrade inventory for existing trees |
| `showcase` / `proof` / `demo` | Diligence + impress packs |

```bash
export FDE_PATH_MEGA_SKILLS=/path/to/mega-skills
export FDE_PATH_GENIUS_MASTERY=/path/to/Genius-Mastery
python -m fde_powerhouse maximize
python -m fde_powerhouse genius --synthesize --role ForwardDeployedAgentic
python -m fde_powerhouse edge-probe --persist --limit 12
python -m fde_powerhouse scan --target .
```

---

## Estate

709 atomic · 33 compound · 29 mega · 10 pipelines · Genius v1.1.0 · 7-gate · 40+ edge · 9 modes

---

## Docs

[DEMO](docs/DEMO.md) · [PROCESS](docs/PROCESS.md) · [GRADE](docs/GRADE.md) · [ARCHITECTURE](docs/ARCHITECTURE.md)

---

*GlacierEQ · Forward Deployed Agentic AI · Maximized*
